# Features

This folder hosts feature-oriented modules (wizard steps, report UI, future 3D globe, etc.).

The 3D scene architecture lives in `src/scene/`, state lives in `src/store/`, and shared UI
components live in `src/ui/`.

